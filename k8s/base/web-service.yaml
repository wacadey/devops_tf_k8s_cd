apiVersion: v1
kind: Service
metadata:
  name: web-service
  labels:
    app.kubernetes.io/name: web
    app.kubernetes.io/part-of: de-ai-12
spec:
  type: NodePort

  selector:
    app.kubernetes.io/name: web

  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
